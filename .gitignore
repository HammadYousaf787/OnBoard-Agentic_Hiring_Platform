# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/

# Each subproject manages its own ignores:
#   frontend/.gitignore  - Next.js (node_modules, .next, .env*, etc.)
#   backend/.gitignore   - Python (.venv, __pycache__, .env, etc.)
